package Communication;

import Agent.Agent;

public class MessageValide extends Message {
    public MessageValide(Agent sender, Agent receiver, Offre offre) {
        super(sender, receiver, offre);
    }
}
